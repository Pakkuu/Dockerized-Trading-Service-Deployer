[project]
name = "risk-check-service"
version = "0.1.0"
description = "FastAPI-based Risk Check Service"
readme = "../README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]
